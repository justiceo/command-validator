// Add a main function that validates only ls command
// This should be a library, so no main function is needed.
// Add a main program in devy-ai that imports the library
// add a pre-exec hook to invoke that main program.
